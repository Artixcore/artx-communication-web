import { TruthfulEmptyState } from '@/components/states/truthful-empty-state'
export default function AccountPage() { return <><p className="artx-kicker">Owner only</p><h1 className="artx-title">Account</h1><TruthfulEmptyState title="Account controls are awaiting API connection" body="Private email, recovery and account-management data will never be embedded into public AID pages." /></> }
