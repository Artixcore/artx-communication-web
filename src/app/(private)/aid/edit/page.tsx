import { TruthfulEmptyState } from '@/components/states/truthful-empty-state'
export default function AidEditPage() { return <><p className="artx-kicker">Owner only</p><h1 className="artx-title">Edit AID</h1><TruthfulEmptyState title="Profile editing is awaiting authenticated API connection" body="Public profile reading and owner-only editing are separate contracts by design." /></> }
